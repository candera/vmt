#!/bin/bash

cd $(dirname $0)/..

STAGE_DIR=~/Dropbox/Public/weathergen

bin/build --allow-dirty
rm -rf ${STAGE_DIR}
mkdir -p ${STAGE_DIR}
cp -R target/ ${STAGE_DIR}/
open https://dl.dropboxusercontent.com/u/451150/weathergen/index.html
