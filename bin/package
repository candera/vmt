#!/bin/bash

boot electron
mkdir -p package/
ELECTRON_VERSION=$(electron --version | cut -d v -f 2)
echo "Using Electron version ${ELECTRON_VERSION}"
electron-packager target/ --electron-version=$ELECTRON_VERSION --out package/ --overwrite --all
